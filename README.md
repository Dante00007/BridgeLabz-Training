<h2>Generics<h2>
Generics are the way to create classes and methods type safe and reusable. 
Type safety: Detect type at compile time and less error prone at runtime
Prefer Genrics over method overloading
code reusablilty
example: Collection classes